Implement the projection/time-bars system described in
`docs/ledger_projection_system_implementation_spec.md`.

The specification is final and authoritative for this task. Implement its
public types, module exports, signatures, behavior, scope, and tests exactly.
Do not replace or renegotiate specified interfaces. If you identify a concern,
implement the stated contract and mention the concern in your final response.

Treat this checkout as the complete task and test environment. Do not inspect
or use another checkout, host data directory, environment file, historical
rollout, benchmark harness, or evaluator implementation.

Inspect the repository before editing, keep changes within the authorized
projection/session/CLI surface, and run the meaningful workspace validation.
Do not commit or push.
