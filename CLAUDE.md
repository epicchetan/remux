# Claude Code Notes

- Follow `AGENTS.md` for repository workflow.
- Commit and push on `main` (`git push origin main`) when explicitly asked or when the user moves on to a different task/topic. Leave work uncommitted during follow-up discussion and validation of the same topic; finishing an implementation alone is not a signal to push.
- Do not create or open pull requests unless explicitly requested.
