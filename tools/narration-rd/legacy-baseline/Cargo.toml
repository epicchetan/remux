[package]
name = "narration-legacy-baseline"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
misaki-rs = { version = "0.3.0", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
unicode-normalization = "0.1"
